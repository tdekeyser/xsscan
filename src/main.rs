mod csrf;
mod session;
mod shared_kernel;
mod error;

fn main() {

    // let http_request = load_request(path);
    // let detector = CsrfDetector::new();
    // let result = detector.check(http_request);
}

#[cfg(test)]
mod tests {
    #[test]
    fn test_all() {}
}
